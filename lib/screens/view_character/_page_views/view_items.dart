import 'package:flutter/material.dart';

class ViewItems extends StatelessWidget {
  const ViewItems({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text(this.toString()));
  }
}
